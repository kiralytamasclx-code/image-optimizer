
  # SVG Optimizer and Viewer

  This is a code bundle for SVG Optimizer and Viewer. The original project is available at https://www.figma.com/design/GGVV2LGKnI8KJusMROoexT/SVG-Optimizer-and-Viewer.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  